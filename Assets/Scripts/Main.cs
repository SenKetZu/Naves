
using UnityEngine;

public class Main : MonoBehaviour
{
    enum estadosMovimiento
    {
        subiendo,
        izq,
        der
    }
    static estadosMovimiento estado;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
